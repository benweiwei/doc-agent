# Screenshots & Demo Assets

Place the screenshots and screen recordings referenced by `README.md` / `README_EN.md` here:

| File | Content suggestion |
|------|--------------------|
| `screenshot-main.png` | Main editor view (document list + editor + instruction bar) |
| `screenshot-diff.png` | AI edit with real-time diff preview (accept / reject) |
| `screenshot-agent.png` | Agent mode tool-call timeline |
| `screenshot-branches.png` | Multi-branch / multi-target view |
| `screenshot-settings.png` | Settings page (style learning & templates) |
| `demo.gif` | Screen recording: type a requirement with no document open → auto-create → AI generates → accept |

Tips:

- PNG for screenshots, GIF (or MP4 linked via GitHub) for recordings.
- Keep each image under ~1 MB so the README loads fast.
- After adding files, commit them — the README image links resolve automatically on GitHub.
